apiVersion: v1
kind: Service
metadata:
  name: "gatling-cp-repository-service"
  namespace: .Values.namespace
  labels:
    helm.sh/chart: "enterprise-locations-packages-1.0.1"
spec:
  selector:
    app.kubernetes.io/name: gatling-cp
  ports:

    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  type: NodePort
